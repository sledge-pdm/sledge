import { onMount } from 'solid-js';
import { pageRoot } from '~/styles/StyleSnippets';

const Update = () => {
  onMount(async () => {});

  return <div class={pageRoot}>update dialog stub</div>;
};

export default Update;
