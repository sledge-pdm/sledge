// Legacy combined test file: kept to avoid surprises in CI. All real tests are split into dedicated files:
// - HistoryActions.color.test.ts
// - HistoryActions.canvasSize.test.ts
// - HistoryActions.imagePool.test.ts
// This file only contains a placeholder test.

import { describe, expect, it } from 'vitest';

describe('Legacy placeholder', () => {
  it('noop', () => {
    expect(true).toBe(true);
  });
});
