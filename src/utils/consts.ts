export const Consts = {
  maxPenSize: 20,
  minCanvasWidth: 1,
  maxCanvasWidth: 5000,
  minCanvasHeight: 1,
  maxCanvasHeight: 5000,
};

export type KeyConfigCommands = 'undo' | 'redo' | 'pen' | 'eraser' | 'fill';
