## frasco移行

### 機能todo

- ~~PenTool+shiftがendでCircleLine描画している CircleLine自体にプレビューの機能が入ってるのでmoveでも適用して問題ない(はず)~~
  ~~ただ改善点としてこれだとこれから形状ごとにXXXLineシェイプを定義せざるを得ないので、オプションにはするものの"start-endに直線を引くプレビュー"という機能はアタッチできるようにした方がいいかもしれない~~
  ~~また後述のJS側ペンプレビューも一旦消えているので、基本形と補完アルゴリズムをまず基本として定義を派生させるのはアリ~~ > ほぼ完了

- ~~Fillが一旦機能しなくなった: Anvilがバッファ持ちなのに対してテクスチャ持ちが基本になったので仕方ない~~
  ~~Gripは基本的にPenのためのもので、かつGL記述でfloodfillはきついものがある anvilのwasm floodfillをそのまんま@sledge/wasmに移行すれば楽~~ > 完了

- ~~Squareの辺がおそらくsize*2-1になっているがおそらくsize-1が正しい　またAnvil時点では偶数サイズに対してはサブピクセルを使っていたが今はどうか要調査＋要テスト記述~~ > 完了

- browser modeでの動作が可能になったので、unit/e2eテストを書いていく　frasco移行箇所だけでなく全機能に対して書いていく

- ~~ImagePoolでwebp変換を利用し続けている > BlobUrl保持などで代用できるかも？~~

- ~~ペンのプレビュー(frasco/grip側は基本形=kernelのマスク出力に対応済み mask_to_pathがwasm実装なので0/1のフラグバッファとはいえ代替の方法があるならそれでもいい)~~ > 完了

### パフォーマンスtodo

- ~~キャンバスリサイズでメモリが増加し続ける（Anvil時代からあったがBufferからTexture依存になったので原因が違いそう　exportRawの結果が常にバインドされて残っていないかなど要確認　そもそもreadpixelsのコストとリークの可能性を考えるとbufferはマジで必要な時以外あんまり持たない方がいいのはある）~~ > wasmのwebp conversionに問題があった　レイヤーバッファ系はgzip移行によりほぼ解決

- ~~4K画像でペンが重い（フルアップロードの影響？）: 若干深堀りの余地あり~~ > ここはThumbnailやその他の最適化で解消 ~~メモリ的には問題ないはずなので旧anvilのようにタイル化も視野だが、FHD程度では問題ないこと、また~~ **GLの最大テクスチャサイズが4K程度で厳しくなること**を考えるとタイルというよりむしろ例えば1024x1024くらいの単位でテクスチャ分割を行う方が理にかなっているかもしれない（ただなんにせよここのサイズは可変になると思われる 問題となるのは最適なタイルの粒度(小or大)）

- ~~若干未検証かついつの間にか治っている可能性があるが、オンボードGPUのみのミドルスペックpcにおいてgetshaderparameterが時間をかけていることがあった > 現状あまり発生しない 関係性は不明だがバッテリ駆動時で基本的に重くなる(電源オプションに関わらず)。単にロースペックではgetshader~がボトルネックということなのか、機種特有の問題かは不明~~ > これもおそらく最適化で解消

- Snapshotは基本的にプロジェクトそのものと同じサイズとみなせるので、2MBのプロジェクトで10回スナップショットを取ると22MBになる(多分。もっとひどいか、もしくはもっと軽い可能性はあるが)。
  これ自体(project in project)にほぼ異存はないのだが、以下の点で疑問が残る
  1. 現状ProjectがMsgpackを使っているが、snapshot自体にもMsgpackをかけるべきかもしれない。
     project.snapshots[i]の内容が親のprojectのmsgpack packing一回でpackedされたことになっているのか疑問が残る。
  2. 読み込み時にsnapshot全体を取得する必要が今のところない。thumbnailとかの情報は入っており、canvasサイズなどのproject内部情報にしてもsnapshot.snapshotから出すか、内部内容を持ち出すにしてもロード時に必要な情報を読みだせたらsnapshotの内容はオンメモリからは捨てても問題ないような気がする